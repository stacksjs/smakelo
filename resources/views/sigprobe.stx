<script server>
const pageTitle = 'mixed probe'
</script>
<!DOCTYPE html>
<html lang="en">
  <head></head>
  <body>
    <div data-stx-content>
      <p id="out">count {{ count() }}</p>
      <button id="go" @click="bump()">bump</button>
    </div>
    <script client>
      import { makeHandlers } from '../functions/probe-factory'

      const count = state(0)

      const { bump } = makeHandlers({ count })
    </script>
  </body>
</html>
