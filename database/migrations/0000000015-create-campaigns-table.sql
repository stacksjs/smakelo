-- qb:generated
-- @generated by `buddy migrate:regenerate` - edits will be overwritten
CREATE TABLE IF NOT EXISTS "campaigns" (
  "id" INTEGER PRIMARY KEY AUTOINCREMENT,
  "name" TEXT not null,
  "description" TEXT,
  "type" TEXT CHECK ("type" IN ('email', 'sms', 'push', 'social', 'multi-channel')) not null,
  "status" TEXT CHECK ("status" IN ('draft', 'scheduled', 'sending', 'sent', 'paused', 'cancelled', 'failed', 'active', 'completed', 'archived')) not null default 'draft',
  "subject" TEXT,
  "template" TEXT,
  "text" TEXT,
  "content" TEXT,
  "channel_settings" TEXT,
  "segment_definition" TEXT,
  "from_name" TEXT,
  "from_address" TEXT,
  "reply_to" TEXT,
  "timezone" TEXT not null default 'UTC',
  "recurrence" TEXT,
  "experiment_metric" TEXT CHECK ("experiment_metric" IN ('open_rate', 'click_rate', 'conversion_rate')),
  "email_list_id" INTEGER REFERENCES "email_lists"("id"),
  "scheduled_at" TEXT,
  "sent_at" TEXT,
  "audience_size" INTEGER default 0,
  "sent_count" INTEGER default 0,
  "open_rate" INTEGER,
  "click_rate" INTEGER,
  "conversion_rate" INTEGER,
  "budget" INTEGER,
  "spent" INTEGER default 0,
  "currency" TEXT not null default 'USD',
  "start_date" TEXT,
  "end_date" TEXT,
  "team_id" INTEGER REFERENCES "teams"("id"),
  "created_at" TEXT not null default CURRENT_TIMESTAMP,
  "updated_at" TEXT,
  "uuid" TEXT
);
CREATE UNIQUE INDEX IF NOT EXISTS "campaigns_uuid_unique" ON "campaigns" ("uuid");
